CONFIG_GPIO=y

# SEGGER RTT console – streams printk/LOG output through the J-Link debug
# connection, no separate UART/USB needed.
CONFIG_UART_CONSOLE=n
CONFIG_RTT_CONSOLE=y
CONFIG_USE_SEGGER_RTT=y